# BirchAtlas 桦聚图集 — 大数据聚类算法应用系统 V2.0 工程重构方案

> **项目名**：BirchAtlas（桦聚图集）
> **文档版本**：v2.0-draft-01
> **编制日期**：2026-08-07
> **文档性质**：面向开发团队的完整工程方案（可直接开工）
> **目标仓库**：GitHub 开源（MIT License）
> **命名由来**：BIRCH（Balanced Iterative Reducing and Clustering using Hierarchies，SIGMOD 1996 经典流式聚类算法）点题系统核心——边缘 BIRCH CF 树流式聚类；Atlas（图集）呼应云端全景聚类与可视化大屏。
> **Slogan**：*Streaming clustering at the edge, unified atlas in the cloud.*

---

## 目录

1. [项目概述](#1-项目概述)
2. [原系统分析与重构动机](#2-原系统分析与重构动机)
3. [核心创新点](#3-核心创新点)
4. [总体架构设计](#4-总体架构设计)
5. [技术选型](#5-技术选型)
6. [系统模块详细设计](#6-系统模块详细设计)
7. [聚类算法引擎设计（核心）](#7-聚类算法引擎设计核心)
8. [边缘智能网关设计](#8-边缘智能网关设计)
9. [数据模型与存储设计](#9-数据模型与存储设计)
10. [通信协议与 API 设计](#10-通信协议与-api-设计)
11. [OTA 远程升级设计](#11-ota-远程升级设计)
12. [安全设计](#12-安全设计)
13. [可观测性设计](#13-可观测性设计)
14. [部署架构](#14-部署架构)
15. [GitHub 仓库规划](#15-github-仓库规划)
16. [开发计划与里程碑](#16-开发计划与里程碑)
17. [工作量估算与团队分工](#17-工作量估算与团队分工)
18. [风险与应对](#18-风险与应对)

---

## 1. 项目概述

### 1.1 项目背景

"大数据聚类算法应用系统 V1.0" 是一套面向工业视觉质检场景（钢筋间距检测）的边缘计算平台，由三部分组成：

| 组成部分 | 原实现 | 定位 |
|---|---|---|
| 边缘网关 | Jetson Xavier NX + Wi-Fi 模组，ROS1(Melodic) 框架 | 现场数据采集、算法推理 |
| 云端服务器 | MQTT/HTTP 协议，设备/项目/测点管理 | 配置下发、指令控制、结果汇聚 |
| 客户端工具 | Delphi 桌面程序（远程控制/配置生成） | 运维管理 |

原系统在**架构形态、算法能力、开发范式**上均已落后于当前主流技术栈，本方案对系统进行**全栈重构**，在保留业务语义（钢筋间距检测、边缘计算、远程升级）的基础上，重塑为**云原生 + 边缘智能 + 大数据聚类算法引擎**的现代平台。

### 1.2 重构目标

1. **架构现代化**：单体 → 微服务 + 云原生（K8s/Docker Compose）
2. **算法实体化**：让"聚类算法"真正成为系统的核心能力（V1.0 中聚类算法名不副实）
3. **边缘智能化**：Jetson 平台 + TensorRT 加速 + 边缘实时聚类
4. **运维自动化**：A/B 双分区 OTA 升级、灰度发布、设备影子
5. **数据资产化**：数据采集 → 数据湖 → 特征工程 → 聚类分析 → 可视化大屏全链路
6. **开源友好**：清晰的分层模块化、完善的 CI/CD、完整的文档体系，可直接托管 GitHub

### 1.3 重构前后对比

| 维度 | V1.0（现状） | V2.0（目标） |
|---|---|---|
| 云端架构 | 单体服务 + HTTP 接口 | 微服务（Spring Cloud / Go）+ 消息队列 |
| 边缘框架 | ROS1 Melodic（已 EOL） | ROS2 Humble + TensorRT 8.x |
| 算法能力 | 仅"钢筋间距检测"单一模型 | 检测模型 + 流式聚类 + 异常检测 + 质量评估 |
| 设备管理 | 手工配置，设备编号硬编码 | 设备影子（Device Shadow）+ 自动注册发现 |
| 远程升级 | 二级 loader + MD5 校验 | A/B 分区 + 断点续传 + 灰度发布 + 自动回滚 |
| 数据传输 | 原始 HTTP POST JSON | MQTT 5.0 + HTTP/2 + 断点续传 + 消息可靠投递 |
| 数据价值 | 结果仅展示 | 数据湖 + 聚类分析 + 可视化大屏 + 报表导出 |
| 客户端 | Delphi 桌面远程工具 | Web 管理控制台（Vue3） |
| 开发语言 | Delphi / C++ | Go / Python / TypeScript / C++ |

---

## 2. 原系统分析与重构动机

### 2.1 V1.0 系统架构回顾

```
┌─────────────────────────────────────────────────────────────┐
│                        云端服务器                            │
│  设备管理 / 项目测点管理 / 算法文件管理 / 指令下发 / 结果汇聚   │
└───────────────────┬──────────────────────────┬──────────────┘
                    │ MQTT / HTTP（移动网络）    │
        ┌───────────▼──────────┐    ┌──────────▼──────────┐
        │  边缘网关             │    │  终端用户 / 客户端工具 │
        │  Jetson Xavier NX    │    │  Delphi 桌面程序     │
        │  Wi-Fi 模组           │    └─────────────────────┘
        └───────────┬──────────┘
                    │ Wi-Fi 局域网（ROS 框架）
        ┌───────────▼──────────┐
        │  采集控制系统（测点）  │
        │  钢筋间距检测装置      │
        └─────────────────────┘
```

### 2.2 V1.0 关键机制（保留并升级）

**（1）设备编号规则** — 保留，升级为自动注册
- 规则：`AA-BBBBBBBB`，AA = 2 位设备类型编码，BBBBBBBB = 8 位序列号
- V2.0：增加设备指纹（硬件序列号 + 公钥）绑定，防伪造

**（2）算法配置下发** — 保留，升级为算法容器化
- V1.0：指定算法文件和模型文件 → 下发给边缘网关
- V2.0：算法包（Oci 镜像 / tar.gz + manifest）→ 注册中心 → 灰度下发 → 边缘热加载

**（3）远程升级（二级 loader）** — 保留核心思想，升级为 A/B 双分区
- V1.0：`config.json` 记录 name + md5，校验通过后启动；文件名规则 `_Vx.y.z[_yyyyMMddhhmmss]`
- V2.0：A/B 双分区 + 升级失败自动回滚 + 增量下载 + 灰度策略（详见第 11 章）

**（4）检测数据协议** — 保留字段语义，升级为物模型驱动
- V1.0：`/monitor/public/api/upload/manual`（人工检测）、`/monitor/public/api/upload/algorithm`（算法检测）
- 字段：`id`(测点), `dev_code`(采集仪编号), `img*_w/h`(图片尺寸), `factor`(mm/px 比例), `high`(拍摄高度), `par1-3`(拍摄参数), `url1-3`(图片地址), `record_time`, `data_list[{type, vals[]}]`（type: 1底横筋/2底纵筋/3面横筋/4面纵筋）
- V2.0：物模型（Thing Model）+ TSDB 时序存储 + 图片对象存储（MinIO/S3）

### 2.3 V1.0 主要痛点

| 痛点 | 影响 | V2.0 对策 |
|---|---|---|
| 单体架构，模块耦合 | 扩展困难、发布风险高 | 微服务 + 领域驱动 |
| ROS1 停止维护 | 安全漏洞、生态萎缩 | ROS2 + DDS |
| 聚类算法名不副实 | 产品定位失实 | 算法引擎实体化（第 7 章） |
| 数据无沉淀 | 检测数据沉睡 | 数据湖 + 分析 + 可视化 |
| 升级机制原始 | 升级失败无回滚 | A/B 分区 OTA |
| Delphi 技术栈过时 | 招人难、维护难 | Web 化 + 云原生 |
| 无安全体系 | 设备可伪造、数据可篡改 | 双向 TLS + 设备认证 + 签名验签 |

---

## 3. 核心创新点

> 面向 GitHub 开源，以下创新点作为 README 亮点与论文/专利素材。

### 3.1 创新点 N1：边缘-云端协同流式聚类引擎（Edge-Cloud Collaborative Streaming Clustering）

- **边缘端**：对钢筋间距检测的原始测量值（vals 数组）做**在线流式聚类**（BIRCH / StreamKM++ / 在线 DBSCAN），实时识别离群测量点（如钢筋间距异常、测量误差），无需等待云端回传。
- **云端**：汇聚多网关数据做**离线全景聚类**（K-Means / 层次聚类 / GMM），识别区域级施工质量分区（如"东区楼板间距普遍偏小"）。
- **协同**：边缘聚类模型（BIRCH 的 CF 树）可**序列化后增量合并**到云端全局模型，实现"边聚类、云融合"。

### 3.2 创新点 N2：算法包即插即用（Algorithm-as-a-Service）

- 算法以**标准化算法包**（manifest.yaml + 模型权重 + 推理代码）上传到算法注册中心。
- 云端对算法包进行**格式校验、签名验证、沙箱测试**后登记。
- 边缘网关支持**热加载**算法包（无需重启），A/B 灰度替换检测模型。
- 算法包内置**自检探针**（健康检查 + 精度自测），不合格自动回退。

### 3.3 创新点 N3：A/B 双分区原子升级 + 自动回滚

- 存储分区 A/B 交替，启动时由 bootloader 选择分区。
- 升级流程：下载 → MD5/SHA256 校验 → 写入备用分区 → 自检（心跳 + 算法自测）→ 切换分区。
- 自检失败自动回滚，全程不影响在线业务（详见第 11 章）。

### 3.4 创新点 N4：设备影子 + 物模型（Thing Model + Device Shadow）

- 每个边缘网关/采集设备在云端有**设备影子**（期望状态 desired / 实际状态 reported / 差分 delta），断网重连自动同步。
- 物模型定义：属性（attribute）、事件（event）、服务（service）、命令（command），实现**数据面与控制面解耦**。

### 3.5 创新点 N5：数据质量驱动的聚类评估闭环

- 引入**聚类质量评估指标**（轮廓系数 Silhouette、DBI 指数、Calinski-Harabasz）实时评估每次聚类结果质量。
- 质量不达标时自动触发**参数自优化**（K 值搜索 / eps 自适应），形成"聚类→评估→优化"闭环。

### 3.6 创新点 N6：全链路可观测（OpenTelemetry + 边缘遥测）

- 云端微服务全链路 Trace + Metrics + Logs（OpenTelemetry + Prometheus + Grafana）。
- 边缘网关遥测：CPU/GPU/温度/内存/网络/推理耗时/聚类耗时，实时上报，异常自动告警。

---

## 4. 总体架构设计

### 4.1 架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                       终端用户层                                      │
│   Web 管理控制台(Vue3) │ 可视化大屏(ECharts) │ 移动端(H5) │ Open API  │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ HTTPS / WSS
┌──────────────────────────────▼──────────────────────────────────────┐
│                       API 网关层 (Kong / APISIX)                      │
│        认证鉴权(JWT/OAuth2) · 限流 · 路由 · 灰度 · 审计                │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────┐
│                         云端微服务层（K8s）                           │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌───────────────────┐ │
│  │ 设备管理    │ │ 项目测点    │ │ 算法注册    │ │ 任务调度            │ │
│  │ device-svc │ │ project-svc│ │ algo-svc   │ │ task-svc          │ │
│  └────────────┘ └────────────┘ └────────────┘ └───────────────────┘ │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌───────────────────┐ │
│  │ 数据接入    │ │ 聚类分析    │ │ 告警通知    │ │ 用户权限            │ │
│  │ ingest-svc │ │ cluster-svc│ │ alert-svc  │ │ auth-svc          │ │
│  └────────────┘ └────────────┘ └────────────┘ └───────────────────┘ │
└──────┬──────────────────┬──────────────────┬────────────────────┬───┘
       │                  │                  │                    │
┌──────▼──────┐   ┌───────▼────────┐  ┌─────▼──────┐   ┌─────────▼───────┐
│ MQTT Broker │   │ 数据湖/存储      │  │ 消息队列    │   │ 对象存储        │
│ EMQX 5.x   │   │ PostgreSQL+TSDB │  │ Kafka      │   │ MinIO/S3       │
│ (MQTT 5.0) │   │ +ClickHouse     │  │            │   │ (图片/模型包)   │
└──────┬──────┘   └────────────────┘  └────────────┘   └─────────────────┘
       │ MQTT 5.0 over TLS（移动网络）
┌──────▼──────────────────────────────────────────────────────────────┐
│                     边缘智能网关层（Jetson）                          │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 边缘网关服务 EdgeGateway (Go)                                   │ │
│  │  设备影子同步 │ 算法包管理器 │ 任务执行器 │ 遥测上报 │ OTA 升级器    │ │
│  └────────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 推理服务 Inference (Python/C++ + TensorRT)                      │ │
│  │ 钢筋间距检测模型 │ 图像预处理 │ 边缘流式聚类(BIRCH) │ 结果生成      │ │
│  └────────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ ROS2 通信层 (Humble)  — DDS 域                                     │ │
│  └────────────────────────────────────────────────────────────────┘ │
└──────┬──────────────────────────────────────────────────────────────┘
       │ Wi-Fi 局域网 (ROS2/DDS)
┌──────▼──────────────────────────────────────────────────────────────┐
│                     采集控制层（现场测点）                            │
│  钢筋间距检测装置 │ 图像采集 │ 传感器 │ 边缘控制 MCU                  │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 分层职责

| 层级 | 组件 | 职责 |
|---|---|---|
| 终端用户层 | Web 控制台 / 大屏 / Open API | 人机交互、数据展示、远程管理 |
| API 网关层 | Kong/APISIX | 统一入口、安全、限流、路由 |
| 云端微服务层 | 8 个核心服务 | 业务逻辑、领域服务 |
| 基础设施层 | EMQX / Kafka / PG / ClickHouse / MinIO | 通信与存储 |
| 边缘网关层 | EdgeGateway + Inference + ROS2 | 现场采集、推理、聚类、升级 |
| 采集控制层 | 检测装置 + 传感器 | 物理世界数据源 |

---

## 5. 技术选型

### 5.1 云端

| 领域 | 选型 | 理由 |
|---|---|---|
| 服务框架 | Go (Gin/Go-zero) 为主，Python (FastAPI) 为辅 | Go 高并发适合 IoT，Python 适合算法 |
| 微服务编排 | Kubernetes + Helm | 云原生标准 |
| 轻量部署 | Docker Compose | 单机/边缘云场景 |
| API 网关 | APISIX（或 Kong） | 高性能、插件丰富 |
| 消息 | MQTT Broker: EMQX 5.x；事件流: Kafka | MQTT 物联协议标准，Kafka 数据总线 |
| 关系库 | PostgreSQL 15 + TimescaleDB | PG 生态 + 时序扩展 |
| 分析库 | ClickHouse | 检测数据聚合分析 |
| 缓存 | Redis 7 | 设备影子、会话、限流 |
| 对象存储 | MinIO（兼容 S3） | 图片、算法包、OTA 包 |
| 认证 | Keycloak（OIDC/OAuth2）+ JWT | 企业级 IAM |
| 可观测 | Prometheus + Grafana + OpenTelemetry + Loki | 全链路 |
| CI/CD | GitHub Actions + ArgoCD | 开源首选 |

### 5.2 边缘端

| 领域 | 选型 | 理由 |
|---|---|---|
| 硬件 | Jetson Orin NX（升级自 Xavier NX） | 更高算力（100 TOPS） |
| OS | Ubuntu 22.04 LTS + JetPack 6.x | LTS 稳定 |
| 通信框架 | ROS2 Humble（DDS/RTPS） | 替代 EOL 的 ROS1 |
| 网关服务 | Go 编写 EdgeGateway 守护进程 | 轻量、跨平台 |
| 推理 | TensorRT 8.x + ONNX Runtime | GPU 加速 |
| 检测模型 | YOLOv8 系列（v8n/v8s）+ 自定义分割头 | 精度/速度平衡 |
| 边缘聚类 | 自研 Go/Python 流式聚类库（BIRCH/StreamKM++） | 轻量在线聚类 |
| 升级 | A/B 双分区 + systemd + 自研 bootloader 脚本 | 原子升级 |

### 5.3 前端

| 领域 | 选型 | 理由 |
|---|---|---|
| 框架 | Vue 3 + TypeScript + Vite | 生态成熟 |
| UI | Element Plus / Ant Design Vue | 组件丰富 |
| 可视化 | ECharts 5 + Mapbox/Leaflet | 大屏与地图 |
| 状态管理 | Pinia | 轻量 |
| 数据请求 | Axios + WebSocket | 实时 |

### 5.4 开发语言分布

| 语言 | 用途 | 占比（预估） |
|---|---|---|
| Go | 云端微服务、边缘网关 | 45% |
| Python | 聚类算法、推理服务、数据管线 | 30% |
| TypeScript | Web 控制台 | 15% |
| C++ | TensorRT 封装、ROS2 节点 | 10% |

---

## 6. 系统模块详细设计

### 6.1 模块全景

```
bigdata-cluster-platform/
├── cloud/                    # 云端微服务
│   ├── auth-svc              # 用户认证授权
│   ├── device-svc            # 设备管理（网关/采集仪）
│   ├── project-svc           # 项目/测点管理
│   ├── algo-svc              # 算法注册与发布
│   ├── task-svc              # 检测任务调度
│   ├── ingest-svc            # 数据接入与清洗
│   ├── cluster-svc           # 聚类分析与评估
│   └── alert-svc             # 告警通知
├── edge/                     # 边缘网关
│   ├── gateway/              # EdgeGateway（Go）
│   ├── inference/            # 推理服务（Python/TensorRT）
│   ├── clustering/           # 边缘流式聚类（Python）
│   └── ros2/                 # ROS2 节点
├── web/                      # Web 控制台（Vue3）
├── proto/                    # gRPC/Protobuf 定义
├── deploy/                   # Helm Charts / Compose
├── docs/                     # 文档
└── examples/                 # 示例与 Demo
```

### 6.2 云端服务详细设计

#### 6.2.1 auth-svc（认证授权）

- 功能：用户管理、角色 RBAC、JWT 签发、OAuth2/OIDC 对接
- 表：`sys_user`、`sys_role`、`sys_user_role`、`sys_permission`、`sys_audit_log`
- 接口：`POST /api/v1/auth/login`、`POST /api/v1/auth/refresh`、`GET /api/v1/users/me`

#### 6.2.2 device-svc（设备管理）

- 功能：
  - 边缘网关与采集仪注册（沿用 `AA-BBBBBBBB` 编号规则 + 设备指纹认证）
  - 设备影子维护（desired/reported/delta）
  - 设备状态监控（在线/离线/升级中）
- 表：`device`、`device_profile`（物模型）、`device_shadow`、`device_ota_record`
- 设备注册时序：
  1. 设备首次上电 → 生成 RSA 密钥对 → 上报公钥 + 硬件指纹 + 预设序列号
  2. 云端校验序列号唯一性 → 签发设备证书（双向 TLS）
  3. 设备凭证书连接 EMQX → 建立设备影子

#### 6.2.3 project-svc（项目测点管理）

- 功能：项目（Project）→ 边缘网关（Gateway）→ 测点（MeasurePoint）三级模型
- 表：`project`、`project_gateway`、`measure_point`
- 测点字段：`id`、`gateway_id`、`name`、`type`、`position`（经纬度/相对坐标）、`algo_id`（绑定的算法）、`params`（JSON）

#### 6.2.4 algo-svc（算法注册中心）

- 算法包结构（tar.gz）：
```
algo-package/
├── manifest.yaml       # 算法元信息（名称/版本/输入输出/资源要求）
├── model/              # 模型权重（.engine / .onnx）
├── inference/          # 推理代码（Python）
├── selfcheck/          # 自检脚本
└── signature.sig       # 签名
```
- manifest.yaml 示例：
```yaml
apiVersion: algo/v2
name: rebar-spacing-yolov8
version: 2.3.1
type: detection            # detection | clustering | hybrid
engine: tensorrt           # tensorrt | onnxruntime
inputs:
  - name: image
    type: image
    format: [jpg, png]
outputs:
  - name: spacing_vals     # 钢筋间距测量值（mm）
    type: float_array
resources:
  min_gpu_mem: 2GB
  min_tensorrt: "8.5"
selfcheck:
  testset: testset/        # 自测数据集
  metric: mAP              # 自测指标
  threshold: 0.85
```
- 发布流程：上传 → 校验签名 → 沙箱测试（精度自测）→ 登记 → 灰度下发

#### 6.2.5 task-svc（任务调度）

- 功能：检测指令下发（单点/批量/定时）、任务状态跟踪、结果回执
- 表：`task`、`task_plan`（定时计划）、`task_result`
- 指令类型：`start_detect`、`stop_detect`、`upload_manual`、`update_algo`、`ota_upgrade`、`reboot`、`get_status`
- 下发链路：task-svc → Kafka → MQTT Bridge → 边缘网关 → 执行 → 回传结果

#### 6.2.6 ingest-svc（数据接入）

- 功能：
  - 接收人工检测/算法检测上报（兼容 V1.0 字段语义）
  - 数据清洗（异常值、重复值、格式校验）
  - 图片转存 MinIO，测量值写入 ClickHouse
  - 数据质量打标（quality_score）
- 接口：`POST /api/v1/upload/manual`、`POST /api/v1/upload/algorithm`（兼容升级）

#### 6.2.7 cluster-svc（聚类分析）

- 功能：见第 7 章
- 表：`cluster_job`、`cluster_result`、`cluster_eval`、`cluster_param_history`

#### 6.2.8 alert-svc（告警通知）

- 功能：检测值越限、聚类质量下降、设备离线、升级失败等事件 → 告警
- 渠道：Web 站内信、企业微信/飞书机器人、邮件、短信（可选）
- 表：`alert_rule`、`alert_event`

### 6.3 前端模块（Web 控制台）

| 页面 | 功能 |
|---|---|
| 登录/权限 | OIDC 登录、角色鉴权 |
| 总览大屏 | 设备总数/在线率、检测量、聚类分区热力图、告警 |
| 设备管理 | 网关/采集仪列表、影子详情、OTA 升级操作 |
| 项目测点 | 项目树、测点配置、算法绑定 |
| 算法中心 | 算法包上传/发布/灰度/版本管理 |
| 检测任务 | 下发指令、任务进度、结果列表 |
| 数据看板 | 检测数据表格、趋势图、聚类可视化 |
| 聚类分析 | 聚类任务创建、参数配置、结果展示（散点/轮廓图） |
| 告警中心 | 告警列表、规则配置 |
| 系统管理 | 用户/角色/审计日志 |

---

## 7. 聚类算法引擎设计（核心）

> 本系统的差异化核心竞争力，也是 GitHub 开源的核心亮点。

### 7.1 算法体系总览

```
                    ┌─────────────────────────────────────┐
                    │         聚类算法引擎                  │
                    │      (cluster-svc / edge clustering) │
                    └───────┬─────────────────┬───────────┘
                            │                 │
              ┌─────────────▼──────┐  ┌───────▼─────────────┐
              │  边缘在线聚类（实时） │  │ 云端离线聚类（全景） │
              │  Streaming BIRCH   │  │ K-Means / GMM      │
              │  Online DBSCAN     │  │ 层次聚类 / 谱聚类    │
              │  StreamKM++        │  │ DBSCAN             │
              └─────────────┬──────┘  └───────┬─────────────┘
                            │                 │
                            │  CF树增量合并    │
                            └───────┬─────────┘
                                    ▼
                    ┌─────────────────────────────────────┐
                    │      聚类结果融合与评估              │
                    │  轮廓系数 / DBI / CH 指数           │
                    │  参数自优化（K 搜索 / eps 自适应）   │
                    └─────────────────────────────────────┘
```

### 7.2 应用场景定义

| 场景 | 算法 | 数据 | 产出 |
|---|---|---|---|
| S1 测量值离群识别 | 边缘流式 BIRCH / Online DBSCAN | 单测点 vals 流 | 实时标注可疑测量点 |
| S2 楼板质量分区 | 云端 K-Means / GMM | 多网关历史间距 | 区域质量热力图 |
| S3 施工工艺分类 | 层次聚类 + 轮廓分析 | 项目级特征 | 施工模式分组 |
| S4 异常趋势发现 | 时间序列聚类（DTW + K-Means） | 时序检测数据 | 异常时段/位置 |
| S5 模型漂移检测 | 聚类分布对比（JS 散度） | 新旧模型输出分布 | 提示模型需要重训 |

### 7.3 边缘流式聚类（核心创新实现）

**方案：BIRCH（Balanced Iterative Reducing and Clustering using Hierarchies）在线版本**

- 数据结构：CF（Clustering Feature）= (N, LS, SS)，三参数可增量更新
- CF 树在线插入复杂度 O(log n)，内存占用固定
- 流程：
  1. 每个测点的检测值 v 进入 CF 树
  2. CF 节点达到阈值 T 时分裂
  3. 定期将叶子 CF 向量（N, LS, SS）序列化上报云端
  4. 云端将多个边缘 CF 子树合并 → 全局聚类

**Go 伪代码（edge/clustering/birch.go 示意）**：
```go
type CF struct {
    N  int       // 点数
    LS []float64 // 线性求和
    SS []float64 // 平方和
}

type CFNode struct {
    CF        CF
    Children  []*CFNode
    IsLeaf    bool
    Radius    float64 // 半径阈值
}

func (n *CFNode) Insert(x []float64, T float64) {
    // 1. 寻找最近的子节点
    // 2. 若插入后半径 <= T，吸收
    // 3. 否则在叶节点新增 CF，必要时分裂
}

// 序列化 CF 树用于云端合并
func (n *CFNode) Serialize() []byte { /* protobuf 编码 */ }
```

**边缘判定逻辑**：
- 新测量值 v 与最近 CF 簇心距离 > k·σ（k 可配，默认 3）→ 标记为"离群候选"
- 连续 3 次离群 → 触发告警 + 提示人工复核

### 7.4 云端离线聚类

| 算法 | 适用场景 | 关键参数 |
|---|---|---|
| K-Means++ | 楼板质量分区 | K（肘部法+轮廓系数自动确定） |
| GMM | 工艺模式混合分布 | 分量数、协方差类型 |
| DBSCAN | 离群点簇、任意形状 | eps（KNN 距离图自适应）、minPts |
| 层次聚类 | 项目/测点分组 | 距离度量、链接准则 |

**聚类任务 JSON 示例**：
```json
{
  "job_id": "cj_20260807_001",
  "dataset": {
    "source": "clickhouse",
    "query": "SELECT dev_code, avg(vals) FROM detect_data WHERE project_id=100 GROUP BY dev_code",
    "features": ["mean_spacing", "std_spacing", "sample_count"]
  },
  "algorithm": "kmeans",
  "params": {
    "k": 0,
    "k_search": {"min": 2, "max": 10, "metric": "silhouette"}
  },
  "output": {"table": "cluster_result", "visualization": true}
}
```

### 7.5 聚类质量评估闭环

| 指标 | 计算 | 用途 |
|---|---|---|
| 轮廓系数（Silhouette） | (b-a)/max(a,b) | 簇内紧致 + 簇间分离综合 |
| DBI（Davies-Bouldin） | 簇内散度/簇间距离 | 越小越好 |
| CH 指数 | 簇间方差/簇内方差 | 越大越好 |
| 稳定度 | Bootstrap 重采样重复聚类的一致性 | 结果可靠性 |

**自优化策略**：
- K-Means：对 K ∈ [2, 10] 计算轮廓系数 → 选择最优 K
- DBSCAN：KNN 距离排序找拐点 → 自适应 eps
- 评估结果存入 `cluster_eval` 表，供可视化与报告

### 7.6 与检测业务的融合输出

- 每次检测完成后，测量值同时进入：ClickHouse（明细）+ 边缘 CF 树（流式聚类）
- 聚类结果为每个测点生成 `quality_grade`（A/B/C/D）+ `outlier_flag`
- 大屏展示：楼板区域散点图（按簇着色）+ 离群点高亮 + 质量等级统计

---

## 8. 边缘智能网关设计

### 8.1 硬件配置（推荐）

| 组件 | 型号 | 说明 |
|---|---|---|
| 计算模组 | Jetson Orin NX 16GB | 100 TOPS |
| 载板 | 自研/第三方 NX 载板 | 双网口、USB3、M.2 |
| 无线 | 4G/5G 模组（EC20/移远 RM520N）+ Wi-Fi6 模组 | 上行 4G/5G，下行 Wi-Fi |
| 存储 | NVMe SSD 512GB | 系统 A/B 分区 + 数据缓存 |
| 电源 | 宽压 DC 9-36V | 工业现场 |
| 接口 | RS485 x2、CAN、GPIO | 对接采集控制系统 |

### 8.2 软件分层

```
┌────────────────────────────────────────────┐
│ EdgeGateway (Go, systemd 守护)             │
│  ├─ ShadowSync   设备影子同步（MQTT）       │
│  ├─ AlgoManager  算法包管理（热加载）       │
│  ├─ TaskExecutor 任务执行器                │
│  ├─ Telemetry    遥测采集上报              │
│  └─ OTAUpdater   A/B 升级                  │
├────────────────────────────────────────────┤
│ Inference Service (Python + TensorRT)      │
│  ├─ Preprocess / Detect / Postprocess      │
│  └─ SpacingCalculator 间距计算(mm)          │
├────────────────────────────────────────────┤
│ Edge Clustering (Python)                   │
│  ├─ BIRCH Stream / Online DBSCAN          │
│  └─ Outlier Detector                       │
├────────────────────────────────────────────┤
│ ROS2 Humble (DDS)                          │
│  ├─ cam_node     图像采集                  │
│  ├─ ctrl_node    采集控制通信              │
│  └─ gw_bridge    网关桥接                  │
└────────────────────────────────────────────┘
```

### 8.3 关键流程

**检测指令执行流程**：
1. 云端 task-svc 下发 `start_detect` 指令（MQTT，QoS 1）
2. EdgeGateway 校验指令签名 → 通知 ROS2 ctrl_node → 采集图像
3. 图像送入 Inference → YOLOv8 检测钢筋 → 计算间距 vals
4. vals 同时进入 Edge Clustering（在线离群判定）+ 结果打包
5. 结果通过 MQTT 上报 ingest-svc（含图片 URL、factor、high 等 V1.0 兼容字段）
6. EdgeGateway 更新设备影子 reported 状态

**断网容灾**：
- 检测结果写入本地 SQLite/文件队列（WAL 模式）
- 网络恢复后按序补传（断点续传）
- 影子同步处理重连期间的 desired 指令

---

## 9. 数据模型与存储设计

### 9.1 PostgreSQL 核心表

```sql
-- 设备表（沿用 AA-BBBBBBBB 规则）
CREATE TABLE device (
  id            BIGSERIAL PRIMARY KEY,
  device_code   VARCHAR(12) UNIQUE NOT NULL,  -- AA-BBBBBBBB
  device_type   SMALLINT NOT NULL,            -- 1=网关 2=采集仪 3=传感器
  name          VARCHAR(64),
  status        SMALLINT DEFAULT 0,           -- 0离线 1在线 2升级中
  fingerprint   VARCHAR(128),                 -- 硬件指纹
  cert_sn       VARCHAR(64),                  -- 设备证书序列号
  project_id    BIGINT,
  position      JSONB,                        -- 经纬度等
  created_at    TIMESTAMPTZ DEFAULT now(),
  updated_at    TIMESTAMPTZ DEFAULT now()
);

-- 设备影子
CREATE TABLE device_shadow (
  device_id   BIGINT PRIMARY KEY REFERENCES device(id),
  desired     JSONB NOT NULL DEFAULT '{}',
  reported    JSONB NOT NULL DEFAULT '{}',
  delta       JSONB NOT NULL DEFAULT '{}',
  version     BIGINT DEFAULT 0,
  updated_at  TIMESTAMPTZ DEFAULT now()
);

-- 项目
CREATE TABLE project (
  id         BIGSERIAL PRIMARY KEY,
  name       VARCHAR(128) NOT NULL,
  desc       TEXT,
  owner_id   BIGINT,
  status     SMALLINT DEFAULT 1
);

-- 测点
CREATE TABLE measure_point (
  id         BIGSERIAL PRIMARY KEY,
  gateway_id BIGINT NOT NULL REFERENCES device(id),
  project_id BIGINT NOT NULL REFERENCES project(id),
  name       VARCHAR(64),
  algo_id    BIGINT,                          -- 绑定的算法
  params     JSONB DEFAULT '{}'
);

-- 算法包
CREATE TABLE algo_package (
  id           BIGSERIAL PRIMARY KEY,
  name         VARCHAR(64) NOT NULL,
  version      VARCHAR(32) NOT NULL,
  type         VARCHAR(16),                   -- detection/clustering/hybrid
  status       SMALLINT DEFAULT 0,            -- 0草稿 1待测 2已发布 3下线
  storage_path VARCHAR(256),                  -- MinIO 路径
  signature    TEXT,
  test_report  JSONB,
  created_at   TIMESTAMPTZ DEFAULT now(),
  UNIQUE(name, version)
);

-- 检测任务
CREATE TABLE task (
  id            BIGSERIAL PRIMARY KEY,
  task_type     VARCHAR(32),                  -- start_detect/ota_upgrade/...
  target        JSONB,                        -- 目标设备/测点
  status        SMALLINT DEFAULT 0,           -- 0待执行 1执行中 2成功 3失败
  payload       JSONB,
  result        JSONB,
  created_at    TIMESTAMPTZ DEFAULT now()
);
```

### 9.2 ClickHouse 检测明细表

```sql
CREATE TABLE detect_data (
  record_time  DateTime64(3) CODEC(Delta, ZSTD),
  project_id   UInt64,
  gateway_code String,
  dev_code     String,       -- 采集仪编号
  measure_point_id UInt64,
  img_url      String,
  factor       Float64,      -- mm/px
  high         Float64,      -- 拍摄高度 m
  data_type    UInt8,        -- 1底横筋 2底纵筋 3面横筋 4面纵筋
  vals         Array(Float64),
  outlier_flag UInt8 DEFAULT 0,
  quality_grade String DEFAULT 'A',
  source       UInt8         -- 0人工 1算法
) ENGINE = MergeTree
PARTITION BY toYYYYMM(record_time)
ORDER BY (project_id, record_time);
```

### 9.3 聚类结果表

```sql
CREATE TABLE cluster_result (
  job_id       String,
  cluster_id   UInt32,
  point_id     UInt64,
  features     Array(Float64),
  label        Int32,
  created_at   DateTime
) ENGINE = MergeTree ORDER BY (job_id, cluster_id);
```

---

## 10. 通信协议与 API 设计

### 10.1 MQTT Topic 规划（MQTT 5.0）

| Topic | QoS | 方向 | 说明 |
|---|---|---|---|
| `{gw}/shadow/desired` | 1 | 云→边 | 期望状态 |
| `{gw}/shadow/reported` | 1 | 边→云 | 实际状态 |
| `{gw}/cmd` | 1 | 云→边 | 控制指令（start_detect 等） |
| `{gw}/cmd/ack` | 1 | 边→云 | 指令应答 |
| `{gw}/data/upload` | 1 | 边→云 | 检测数据上报 |
| `{gw}/algo/{version}` | 1 | 云→边 | 算法包分发 |
| `{gw}/ota` | 1 | 云→边 | OTA 指令 |
| `{gw}/telemetry` | 0 | 边→云 | 遥测（可丢） |
| `{gw}/alarm` | 1 | 边→云 | 边缘告警 |

### 10.2 REST API（与 V1.0 兼容升级）

**人工检测上传**（兼容 V1.0 `/monitor/public/api/upload/manual`）：
```
POST /api/v1/upload/manual
{
  "id": 1001,              // 测点id
  "dev_code": "BB-00000001",
  "img1_w": 1920, "img1_h": 1080,
  "img2_w": 1920, "img2_h": 1080,
  "img3_w": 1920, "img3_h": 1080,
  "factor": 0.85,          // mm/px
  "high": 3.2,             // 拍摄高度 m
  "par1": "", "par2": "", "par3": "",
  "url1": "https://oss/...", "url2": "", "url3": "",
  "record_time": "2026-08-07 10:30:00"
}
→ { "code": 1, "msg": "ok", "data": {"record_id": 88321} }
```

**算法检测上传**（兼容 V1.0 `/monitor/public/api/upload/algorithm`）：
```
POST /api/v1/upload/algorithm
{
  "id": 1001, "dev_code": "BB-00000001",
  "img1_w": 1920, "img1_h": 1080,
  "img2_w": 1920, "img2_h": 1080,
  "img3_w": 1920, "img3_h": 1080,
  "factor": 0.85, "high": 3.2,
  "par1": "", "par2": "", "par3": "",
  "url1": "...原图", "url2": "...算法检测图", "url3": "...楼板钢筋图像",
  "record_time": "2026-08-07 10:31:00",
  "data_list": [
    {"type": 1, "vals": [30.1, 40.5, 31.2, 33.1, 31.5]},
    {"type": 2, "vals": [85.2, 84.8, 86.1]}
  ]
}
→ { "code": 1, "msg": "ok", "data": {"record_id": 88322, "outlier_hint": "测点1001 type=1 存在3个离群值"} }
```

### 10.3 新增 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/v1/cluster/jobs | 创建聚类任务 |
| GET | /api/v1/cluster/jobs/{id} | 查询聚类结果 |
| POST | /api/v1/algo/packages | 上传算法包 |
| POST | /api/v1/algo/packages/{id}/publish | 发布算法 |
| POST | /api/v1/algo/packages/{id}/gray | 灰度下发 |
| POST | /api/v1/devices/{code}/ota | 发起 OTA |
| GET | /api/v1/devices/{code}/shadow | 查询设备影子 |
| GET | /api/v1/dashboard/overview | 总览大屏数据 |
| POST | /api/v1/alert/rules | 告警规则配置 |

---

## 11. OTA 远程升级设计

### 11.1 A/B 双分区架构

```
/dev/mmcblk0p1  →  system_a   （当前运行）
/dev/mmcblk0p2  →  system_b   （备用）
/dev/mmcblk0p3  →  data       （配置/日志/缓存）
```

### 11.2 升级流程

```
1. 云端发布新版本 (ota-svc) → 生成升级包 + SHA256 + 签名
2. 灰度规则：按设备组/比例/时间窗下发
3. 边缘 OTAUpdater 下载升级包（断点续传）
4. 校验 SHA256 + 验签 → 解包 → 写入 system_b
5. 更新 bootloader 标志（boot_once=b, retry=3）
6. 重启 → 从 system_b 启动
7. 自检：EdgeGateway 启动 → 上报心跳 → 算法自测（跑测试集）
   ├─ 通过 → 确认切换，boot_once 清除
   └─ 失败（retry>0）→ 回滚到 system_a，上报失败原因
```

### 11.3 升级包格式

```
ota-package/
├── manifest.json       # 版本/平台/依赖/灰度
├── rootfs.tar.zst      # 根文件系统增量
├── edgegateway         # 网关程序
├── algorithms/         # 内置算法包
├── sha256sums.txt
└── signature.sig       # 云私钥签名
```

### 11.4 灰度策略

| 阶段 | 范围 | 条件 |
|---|---|---|
| 金丝雀 | 1 台内测网关 | 人工确认 |
| 小批量 | 5% 设备组 | 遥测正常 24h |
| 大批量 | 30% | 无回滚事件 |
| 全量 | 100% | 稳定 72h |

---

## 12. 安全设计

| 层面 | 措施 |
|---|---|
| 传输安全 | 云端-边缘 MQTT over TLS 1.3；Web HTTPS/WSS |
| 设备认证 | 一机一密：设备 RSA 密钥对 + 证书绑定，双向 TLS |
| 设备防伪 | 硬件指纹（eMMC 序列号+安全芯片 SN）+ 编号唯一性 |
| 指令防篡改 | 云端指令 RSA 签名，边缘验签 |
| 算法包安全 | 算法包签名校验 + 沙箱运行 + 资源限制（cgroup） |
| 数据安全 | 敏感字段加密存储（AES-256），图片访问签名 URL |
| 应用安全 | RBAC + JWT 短时过期 + 刷新令牌；API 限流防刷 |
| 审计 | 全操作审计日志（sys_audit_log），不可篡改 |

---

## 13. 可观测性设计

| 维度 | 云端 | 边缘 |
|---|---|---|
| Metrics | Prometheus + Grafana：QPS/延迟/错误率/队列积压 | 遥测上报：CPU/GPU/温度/内存/网络/推理耗时/聚类耗时 |
| Logs | Loki + EFK 集中日志 | journald + 本地滚动文件，定期上传 |
| Traces | OpenTelemetry + Jaeger 全链路 | EdgeGateway span 上报 |
| 告警 | Alertmanager：服务降级/存储水位/设备批量离线 | 边缘自愈：重启服务/回滚算法/降级策略 |

---

## 14. 部署架构

### 14.1 生产环境（K8s）

```
K8s Cluster (3 Master + N Worker)
├── emqx-operator     → EMQX 集群
├── kafka             → 事件总线
├── postgresql+tsdb   → 主库
├── clickhouse        → 分析库
├── redis             → 缓存
├── minio             → 对象存储
├── keycloak          → IAM
├── cloud-services    → 8 个微服务
├── prometheus/grafana/loki/jaeger → 可观测
└── argocd            → GitOps 发布
```

### 14.2 开发/演示环境（Docker Compose）

```yaml
services:
  emqx:        image: emqx/emqx:5.8
  postgres:    image: timescale/timescaledb:2.15-pg15
  clickhouse:  image: clickhouse/clickhouse-server:24
  redis:       image: redis:7
  minio:       image: minio/minio
  kafka:       image: bitnami/kafka:3.7
  auth-svc:    build: ./cloud/auth-svc
  device-svc:  build: ./cloud/device-svc
  # ... 其余服务
  web:         build: ./web
  # 边缘模拟器（开发用，模拟网关行为）
  edge-sim:    build: ./examples/edge-sim
```

### 14.3 边缘网关部署

```bash
# install.sh 安装流程
1. 烧录 Ubuntu 22.04 + JetPack 6.x（A/B 双分区）
2. 安装 ROS2 Humble、CUDA、TensorRT、EdgeGateway 包
3. 配置 /etc/edgegateway/config.yaml（云端地址、设备证书）
4. systemctl enable edgegateway --now
5. 首次启动自动注册 → 绑定项目测点
```

config.yaml 示例：
```yaml
cloud:
  mqtt:
    host: mqtt.example.com
    port: 8883
    tls: true
  http:
    base: https://api.example.com
device:
  code: "GW-00000001"        # AA-BBBBBBBB
  fingerprint: "..."          # 自动生成
algo:
  auto_load: true
  cache_dir: /data/algorithms
ota:
  partition: /dev/mmcblk0p2
  max_retry: 3
telemetry:
  interval: 30s
```

---

## 15. GitHub 仓库规划

### 15.1 仓库结构（Monorepo）

```
bigdata-cluster-platform/          # 根仓库
├── README.md                      # 项目介绍 + 架构图 + 亮点 + 快速开始
├── LICENSE                        # MIT
├── CONTRIBUTING.md
├── SECURITY.md
├── docs/
│   ├── architecture.md            # 架构说明
│   ├── clustering-algorithm.md    # 聚类算法设计文档
│   ├── mqtt-protocol.md           # MQTT 协议规范
│   ├── api/                       # OpenAPI 规范
│   └── deployment/                # 部署指南
├── cloud/                         # 云端微服务（Go/Python）
├── edge/                          # 边缘网关（Go/Python/C++/ROS2）
├── web/                           # Web 控制台（Vue3）
├── proto/                         # gRPC 定义
├── deploy/
│   ├── docker-compose.yml
│   └── helm/
├── .github/
│   ├── workflows/                 # CI/CD（build/test/lint/release）
│   └── ISSUE_TEMPLATE/
├── examples/
│   ├── edge-sim/                  # 边缘网关模拟器（演示用）
│   ├── dataset/                   # 示例数据集（合成钢筋间距数据）
│   └── notebooks/                 # Jupyter 聚类算法演示
└── Makefile
```

### 15.2 CI/CD 流水线（GitHub Actions）

```yaml
name: CI
on: [push, pull_request]
jobs:
  lint-test:
    - go vet ./... && go test ./...
    - pytest edge/inference
    - npm run lint && npm run test
  build:
    - docker build cloud/* -t ghcr.io/...
    - docker build edge/gateway -t ghcr.io/...
  release:
    - 打 tag vX.Y.Z 自动发布 docker 镜像 + Helm chart
```

### 15.3 README 亮点段落（建议）

```markdown
# BirchAtlas 桦聚图集

> Streaming clustering at the edge, unified atlas in the cloud.
> 边缘流式聚类 · 云端全景图集

## ✨ 核心亮点
- 🧠 **边缘-云端协同流式聚类**：BIRCH CF 树边端实时聚类，云端增量合并（SIGMOD 1996 经典算法工程化）
- 📦 **算法即插即用**：标准化算法包 + 沙箱验证 + 灰度热加载
- 🔄 **A/B 双分区 OTA**：升级原子化，失败自动回滚
- 🌥️ **云原生微服务**：Go + K8s + EMQX，可水平扩展
- 📊 **聚类质量闭环**：轮廓系数/DBI 驱动的参数自优化
- 🛰️ **全链路可观测**：OpenTelemetry + 边缘遥测
```

### 15.4 开源协议与合规

- 协议：MIT（主仓库）；算法模型权重单独声明 License（如 Apache 2.0）
- 数据集：合成数据（开源） + 真实数据脱敏后按需提供
- 安全：GitHub Security Advisories + SECURITY.md 漏洞报告流程

---

## 16. 开发计划与里程碑

### 阶段 P0：基础设施与核心框架（第 1-4 周）
- [ ] Monorepo 骨架、proto 定义、CI 流水线
- [ ] Docker Compose 全栈拉起（EMQX/PG/CH/Redis/MinIO/Kafka）
- [ ] auth-svc + device-svc（设备注册/影子）
- [ ] Web 控制台基础框架（登录/设备列表）

### 阶段 P1：业务闭环（第 5-10 周）
- [ ] project-svc（项目/测点）、task-svc（指令下发）
- [ ] ingest-svc（上传接口，兼容 V1.0 字段）
- [ ] EdgeGateway v1（MQTT 连接/影子/指令执行/遥测）
- [ ] Inference v1（YOLOv8 钢筋检测 + TensorRT）
- [ ] 端到端 Demo：采集→检测→上传→展示

### 阶段 P2：聚类算法引擎（第 11-16 周）
- [ ] 边缘流式聚类库（BIRCH/Online DBSCAN）
- [ ] cluster-svc（离线聚类 + 质量评估 + 参数自优化）
- [ ] 聚类可视化（散点/轮廓/热力图）
- [ ] 算法注册中心（算法包上传/沙箱/灰度）

### 阶段 P3：OTA 与安全加固（第 17-22 周）
- [ ] A/B 分区升级 + 灰度发布
- [ ] 设备证书体系 + 指令签名
- [ ] 告警中心（多通道）
- [ ] 大屏可视化

### 阶段 P4：开源打磨（第 23-26 周）
- [ ] 文档完善、示例数据集、edge-sim 模拟器
- [ ] 性能压测、稳定性测试
- [ ] GitHub Release v1.0.0
- [ ] 论文/技术博客输出（聚类算法亮点）

---

## 17. 工作量估算与团队分工

| 角色 | 人数 | 负责 | 周期 |
|---|---|---|---|
| 架构师/后端负责人 | 1 | 架构、proto、device/task/ingest | 全程 |
| Go 后端开发 | 2 | 微服务实现 | 全程 |
| 算法工程师 | 1-2 | 检测模型、聚类引擎、质量评估 | P1-P3 |
| 边缘开发（C++/ROS2） | 1-2 | EdgeGateway、TensorRT、OTA | P1-P3 |
| 前端开发 | 1-2 | Web 控制台、大屏 | P0-P3 |
| 运维/DevOps | 1 | CI/CD、K8s、可观测 | 全程 |
| 测试 | 1 | 集成测试、压测、灰度验证 | P2-P4 |

**总规模预估**：6-8 人团队，26 周左右（不含需求冻结期）。

---

## 18. 风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| ROS1→ROS2 迁移成本 | 中 | 采集控制层接口抽象，桥接层过渡 |
| TensorRT 模型精度损失 | 中 | INT8 校准 + FP16 兜底，精度回归测试 |
| 边缘算力不足 | 低 | Orin NX 100TOPS 富余；模型可选 v8n |
| 断网数据积压 | 中 | 本地队列 + 断点续传 + 容量水位告警 |
| 开源后安全暴露面 | 中 | 一机一密、指令签名、漏洞披露流程 |
| 多设备异构兼容 | 中 | 物模型抽象 + 驱动插件化 |

---

## 附录 A：与 V1.0 软著材料的映射

| V1.0 材料内容 | V2.0 落点 |
|---|---|
| 设备编号 AA-BBBBBBBB | device.device_code + 指纹认证 |
| 参数配置（读写绑定关系、控制采集） | 设备影子 desired/reported + task-svc |
| 算法配置（指定算法/模型文件下发） | algo-svc 算法包注册 + 灰度下发 |
| 远程升级（二级 loader + MD5） | OTA A/B 双分区 + SHA256 + 灰度回滚 |
| 上传人工检测接口 | /api/v1/upload/manual（兼容） |
| 上传算法检测接口（含 data_list） | /api/v1/upload/algorithm（兼容，新增 outlier_hint） |
| 采集控制系统 Wi-Fi 局域网（ROS） | ROS2 Humble + DDS |
| Jetson Xavier NX 硬件 | Jetson Orin NX（向上兼容） |
| Delphi 客户端工具 | Web 控制台（Vue3）替代 |

## 附录 B：演示 Demo 建议（GitHub 快速上手）

1. `docker compose up -d` 拉起云端全栈
2. `make edge-sim` 启动边缘网关模拟器（随机生成钢筋间距数据）
3. 打开 Web 控制台 → 观察设备上线 → 下发检测指令 → 数据入湖
4. 创建聚类任务 → 查看散点图与质量评估
5. `make demo-ota` 模拟 A/B 升级回滚

---

*方案完 — 欢迎技术评审与修订建议。*
